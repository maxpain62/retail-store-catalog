apiVersion: v1
kind: Service
metadata:
  name: retail-store-catalog-service
  labels:
    app: retail-store-catalog
spec:
  selector:
    app: retail-store-catalog
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort