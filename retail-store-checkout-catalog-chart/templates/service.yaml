apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-store-checkout-catalog-chart.fullname" . }}
  labels:
    {{- include "retail-store-checkout-catalog-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retail-store-checkout-catalog-chart.selectorLabels" . | nindent 4 }}
